# V3 检索模块流程图

本文描述 **`run_once` 检索阶段**（从配置到进入 `PipelineRunner` 逐条处理之前）的数据流，与代码实现一致。

**时间窗与发布时间**：合并去重后使用 `filter_by_age(candidates, MAX_ARTICLE_AGE_HOURS)`。`published_at` 为空、仅空白、或无法解析为时间的 `SearchHit` **一律不进入候选**；`max_hours <= 0` 时不做小时比较，但仍须可解析的 `published_at`。

**主要代码入口**：`src/pipeline.py` → `PipelineRunner.run_once`（检索段）；`src/search.py` → `filter_by_age`、`merge_search_hits`；`src/rss_aggregate.py`；`src/search.py` → `search_gdelt_topics_bilingual` 或 `search_with_toolchain`（站点扇出模式）。

---

```mermaid
flowchart TB
  subgraph inputs [配置与词表]
    DS[data_sources.json<br/>RSS / html_list 源]
    KW[search_keywords.json<br/>chinese_keywords / english_keywords<br/>及可选站点词等]
    ENV[".env：MAX_ARTICLE_AGE_HOURS<br/>RSS_* / GDELT_* / SEARCH_*"]
  end

  subgraph phase0 [Phase 0：RSS 聚合]
    P0{rss_aggregate_enabled?}
    P0 -->|是| AGG[aggregate_rss_from_data_sources]
    AGG --> RSS1[拉各 RSS XML]
    AGG --> HTML[拉 list_monitor_url 栏目页]
    HTML --> HLM["html_list_monitor：<br/>稿件链 + 列表块时间 / meta / 文本日期 / URL t 日期等"]
    RSS1 --> PARSE["_parse_feed_items：<br/>pubDate / DC date 等"]
    PARSE --> REL1{rss_relevance_filter?}
    HLM --> REL2{rss_relevance_filter?}
    REL1 --> RSSHITS["SearchHit（from_rss=true）"]
    REL2 --> RSSHITS
    P0 -->|否| RSSHITS0["rss_hits 为空列表"]
  end

  subgraph phase1 [Phase 1：GDELT]
    BR{"gdelt_site_scoped_enabled<br/>且 target_sites 非空?"}
    BR -->|是| SITE["按站点查询词 × domain:site<br/>search_with_toolchain"]
    SITE -->|有结果| SITEHITS[site_phase_hits]
    SITE -->|全无| GLOB1[全域分类词 GDELT]
    BR -->|否| BILING["search_gdelt_topics_bilingual<br/>中英 OR 批查询 + 限速/重试"]
    GLOB1 --> GDHITS[global_phase_hits]
    BILING --> GDHITS
  end

  subgraph merge [合并与候选]
    MERGE[merge_search_hits<br/>按 URL 去重池上限]
    CANON["_canonical_url<br/>按规范化 URL 去重"]
    AGE["filter_by_age<br/>须非空且可解析 published_at<br/>且落在 MAX_ARTICLE_AGE_HOURS 内"]
    CAP["candidates[: max_publish_per_run, 50]"]
  end

  inputs --> phase0
  inputs --> phase1
  RSSHITS --> MERGE
  RSSHITS0 --> MERGE
  SITEHITS --> MERGE
  GDHITS --> MERGE
  MERGE --> CANON --> AGE --> CAP

    CAP --> NEXT["后续 pipeline：见 docs/入库与事件聚类流程.md<br/>逐条 _fetch_text（trafilatura 优先）→ 门禁与库去重 → 可选向量事件聚类 → 摘要/中译 → events + articles 落库"]
```

---

*文档版本：与 V3 检索逻辑同步；若 `run_once` 分支有调整，请一并更新本图。*
